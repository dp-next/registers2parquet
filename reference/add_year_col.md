# Add year column to data if possible

Add year column to data if possible

## Usage

``` r
add_year_col(data)
```

## Arguments

- data:

  A data frame with a `source_file` column to extract year from.

## Value

A data frame with a year column added if the year could be determined.
