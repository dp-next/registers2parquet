# Read a Parquet file as DuckDB table

Read a Parquet file as DuckDB table

## Usage

``` r
read_register_file(file_path)
```

## Arguments

- file_path:

  A character scalar with the path to the Parquet file.

## Value

The register as a DuckDB table.
