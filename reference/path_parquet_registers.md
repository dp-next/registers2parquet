# Path to Parquet registers

Path to Parquet registers

## Usage

``` r
path_parquet_registers(id = "708421")
```

## Arguments

- id:

  The project ID.

## Value

Path to the Parquet registers within the project.

## Examples

``` r
if (FALSE) { # \dontrun{
path_parquet_registers()
} # }
```
