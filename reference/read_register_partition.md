# Read a partitioned Parquet register as DuckDB table

Read a partitioned Parquet register as DuckDB table

## Usage

``` r
read_register_partition(dir_path)
```

## Arguments

- dir_path:

  A character scalar with the path to the Parquet register directory.

## Value

The register as a DuckDB table.
