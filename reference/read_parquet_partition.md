# Read an individual Parquet partitioned database

Read an individual Parquet partitioned database

## Usage

``` r
read_parquet_partition(dir)
```

## Arguments

- dir:

  The folder that holds the Parquet database.

## Value

A DuckDB database connection.
